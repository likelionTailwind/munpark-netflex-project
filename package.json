{
  "scripts": {
    "start": "run-p serve watch",
    "serve": "live-server ./src/ --host=localhost --port=8090",
    "compile": "tailwindcss -i src/css/tailwind.css -o src/css/style.css",
    "watch": "npm run compile -- --watch"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.2.4"
  }
}